%h1 タスク一覧表
- @tasks.each do |task|
  %table
    %thead
      %tr
        %th タスク名
        %th 優先度
        %th 終了予定日
    %tbody
      %tr
        %td= link_to task.title, task_path(task)
        %td= task.priority
        %td= task.finish_at

= link_to "新規作成", new_task_path
